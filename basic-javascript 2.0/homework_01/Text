1. Змінна записується за допомогою заданого імені у відповідному фрагменті коду.  Приклад: let name = ''

2. Prompt створенний щоб вивести інформацію для користувача, та отримати якісь данні від нього, щоб потім повернути їх значення або null.

   Confirm створенний щоб просто вивести інформацыю для користувача, та чекае поки він вибере «OK» чи «CANCEL», повертае true/false.

3. Це коли Javascript автоматично приведе до одного типу, приклад:

   123 + '321' - ми отримаемо значення '123321', Javascript автоматично привыв значення до типу String.


