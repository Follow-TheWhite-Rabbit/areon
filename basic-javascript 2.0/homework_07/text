
1. Метод forEach приймае у серединну callBack функцыю, яку використовуе (виконуе), для кожного елементу масиву.

2. Щоб очистити масив від елементів, потрібно встановити його довжину на нуль. (Array.length = 0) Ця операція видаляє всі елементи масиву, і навіть їх значення.

3. Метод Array. isArray() возвращает true , если объект является массивом и false , если он массивом не является.